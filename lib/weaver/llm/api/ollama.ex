defmodule Weaver.Api.Ollama do
  # TODO: handle api calls in here
  # this is the layer that will do transformation from the input format once it becomes required.
  # This is a library, not a genserver
end
