#!/usr/bin/env -S nu --stdin

export def main []: string -> string {
  let input = $in | from json
  let params = $input.tool_call.function.arguments

  {
    url: $params.url
    formats: ["markdown"]
    timeout: 60000
  }
  | to json
  | http post http://localhost:3002/v2/scrape
  | get data.markdown
}
